#When will the else part of try-except-else be executed? 
"""In a try-except-else block in Python, the else block will be executed 
if no exceptions are raised in the try block. The else block is optional, and is executed immediately after the
try block if no exceptions were raised. Here's an example"""
